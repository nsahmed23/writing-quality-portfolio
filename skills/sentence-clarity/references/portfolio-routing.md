# Portfolio routing excerpt

Run global-to-local: `doc-typing` when technical documentation → `memo-structure` when argument-led → `cohesion-emphasis` → `sentence-clarity` → `concision` → `sentence-variety` → targeted `usage-adjudicator`.

Explicit narrow wording invokes only the named fault class. Broad polish begins with a level-by-level diagnosis and skips passes with no fault. Structural edits invalidate later local edits; local edits do not justify reopening structure unless they expose a logical gap.
